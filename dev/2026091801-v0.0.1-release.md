# Feature: VaultPub v0.0.1 release

## Goal

Align the package version with the requested `0.0.1` release, generate release notes from the changes since `v1.0.0`, and publish the tagged GitHub release.

## Conclusion

Updated the package metadata and runtime version to `0.0.1`. Release notes summarize the recent responsive-sidebar, slide, typography, embedding, and input improvements.

## Changed Files

- `pyproject.toml`
- `src/vaultpub/__init__.py`
- `dev/2026091801-v0.0.1-release.md`

## Tests

- Build the `0.0.1` wheel.
- Run focused web, Django, static-export, and package-version checks.
- Verify the GitHub tag and release target the version-bump commit.

## Manual Verification

- Confirm the `v0.0.1` tag, release notes, and wheel asset on GitHub.
