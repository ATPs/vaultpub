# Feature: VaultPub v1.0.1 release correction

## Goal

Correct the release version to follow the existing `v1.0.0` release after the initially requested `0.0.1` number was clarified as `1.0.1`.

## Conclusion

Updated package metadata and runtime version to `1.0.1`, built a fresh wheel, and prepared the release from the same recent changes. The earlier public `v0.0.1` release remains unchanged.

## Changed Files

- `pyproject.toml`
- `src/vaultpub/__init__.py`
- `dev/2026091802-v1.0.1-release-correction.md`

## Tests

- Build the `1.0.1` wheel.
- Run focused web, Django, static-export, and package-version checks.
- Verify the GitHub tag and release target the correction commit.

## Manual Verification

- Confirm the `v1.0.1` tag, release notes, and wheel asset on GitHub.
